# Database (SQLite for dev; swap for a Postgres URL in production)
DATABASE_URL="file:./dev.db"

# Auth session signing secret (change in production!)
AUTH_SECRET="change-me-to-a-long-random-secret-string-in-production"
